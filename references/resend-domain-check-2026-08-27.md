Resend 網域頁面核對：已登入帳戶 arnoldchan2150，inbetweenday.com 狀態為 failed。SPF MX（send → feedback-smtp.eu-west-1.amazonses.com，priority 10）及 SPF TXT（send → v=spf1 include:amazonses.com ~all）均顯示 Verified。DKIM TXT Name 為 resend._domainkey，Value 與使用者提供的 p=MIG... 相符，但狀態為 Failed，Resend 顯示 Missing DKIM record，要求在修正 DNS 後按 restart verification。下一步需確認 Cloudflare 權威 DNS 是否實際公開該 TXT，並核對是否有 CNAME 衝突或錯誤／重複網域名稱。
已在 Resend 網域頁按 Restart。頁面狀態由 failed 轉為 pending，顯示 Looking for DNS records，並提示 Cloudflare propagation 可能需要數小時。DKIM、SPF MX、SPF TXT 暫時均顯示 Pending；這是重新驗證進行中，不是新的 Failed 結論。公共 DNS 三個查詢已回傳 DKIM TXT Value。下一步等待 Resend 完成非同步驗證後重新查看。
Resend 最終驗證成功：2026-08-27 12:21 PM，inbetweenday.com Status = verified，頁面顯示 Domain verified: Your domain is ready to send emails。DKIM TXT resend._domainkey = Verified；SPF MX send（priority 10）= Verified；SPF TXT send = Verified。
正式環境訂閱測試診斷：production logs 顯示 `[Email] Forge send failed: 404 404 page not found`，沒有出現 Resend API send log。這證明該部署實例未讀到 RESEND_API_KEY，因而落入 Forge 備援路徑；前台此前仍顯示成功，形成誤導。下一步需確保 RESEND_API_KEY 隨正式部署注入，並部署已更新的 confirmationEmailSent 回傳邏輯。
