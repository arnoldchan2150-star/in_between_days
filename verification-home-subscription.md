

## 首頁訂閱區塊驗證

首頁抽取內容已確認顯示「立即訂閱」、「把新的旅程寄到你的信箱」，以及「訂閱 In-Between Days，日後有新的遊記、滑雪攻略與旅遊靈感，我會透過 Email 與你分享。」按鈕為可互動的 `button`，不是原本的 `/booklet` 連結。首頁全頁預覽的版面仍保持完整。


瀏覽器已定位到首頁的「立即訂閱」按鈕；元素索引點擊後仍維持按鈕畫面，下一步以目前 viewport 座標再次觸發，以排除預覽工具索引定位問題。


DOM 與畫面驗證結果：以 DOM 觸發「立即訂閱」後，首頁表單成功展開，出現 `你的姓名` 文字欄位、`你的 Email` Email 欄位、`立即訂閱` 提交按鈕及 `取消` 按鈕。預覽工具的視覺編輯遮罩會攔截一般索引點擊，因此使用 DOM 事件完成互動驗證。
